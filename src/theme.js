import { createTheme } from "@mui/material";

export const theme = createTheme({
	palette:{
		primary: {
			main: "#371B58",
			light: "#ffdf00"
		},
		secondary:{
			main: "#C0C0C0",
			light: "#D3D3D3"
		}
	}
})